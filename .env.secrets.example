# Optional overrides — leave blank to auto-fetch from CLI logins
# Copy to .env.secrets only when you need to override auto-fetch

NEON_PROJECT_ID=
FLY_API_TOKEN=
CF_API_TOKEN=
DB_CONNECTION_URI=
BAO_TOKEN=
